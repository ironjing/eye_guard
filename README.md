# Mac Eye Guard

一个 macOS 菜单栏护眼小工具。

默认规则：
- 工作 20 分钟
- 休息 20 秒
- 每隔 3 次休息进入 5 分钟长休

休息期间会在所有屏幕显示黑色遮罩，展示倒计时和 `已休息好` 按钮。

## 运行

```bash
swift run
```

如果本机有 Xcode，也可以直接在 Xcode 打开本目录运行。

## 可配置项

在菜单栏中打开 `设置`：
- 工作时长（分钟）
- 短休时长（秒）
- 长休时长（分钟）
- 每隔几次进入长休
